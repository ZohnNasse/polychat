# PolyChat 작업 세션 브리핑

> polychat을 실제로 짓는 세션(모델이든 사람이든)을 위한 방향 메모. overseer 세션이 남긴다.

작성. 2026-06-20

## 한 줄 비전

House M.D.식 감별진단 엔진. 사용자 = 환자. 모델 팀이 진단한다.

## MVP 범위 (이것만 만든다)

1. 입력칸 1개 — 환자의 증상(질문·맥락)
2. 팬아웃 → 서로 다른 모델 3개에 고정 역할. 동조 / 반대 / 엉뚱
3. 3열로 답 나란히
4. 마지막 합성 1회 = "검증할 가설 N개" + 레드팀 반박 한 단락

뺀 것. 의장 다단계 루프, 상시 레드팀, 테스트 자동생성, DB, 멀티턴. 전부 나중.

## MVP가 검증할 단 하나의 가정

역할을 박은 멀티모델 회의가 진짜 쓸 divergence를 내는가, 아니면 그럴듯한 죽인가. 이거 하나만 답하면 MVP는 성공이다. 재미없으면 접고, "이거다" 싶으면 키운다.

## 절대 지킬 설계 제약

- 역할은 반드시 다른 베이스 모델(Claude/GPT/Gemini)에 박아라. 같은 모델에 프롬프트만 바꾸면 sycophancy로 동조 수렴 → 역할 붕괴. 이게 polychat이 멀티프롬프트가 아니라 멀티모델이어야 하는 이유다.
- 출력은 결론이 아니라 "검증할 가설"이다. 모델은 모르는 업계를 사용자만큼 자신 있게 지어낸다. 합의를 현실로 믿으면 SME 실패를 더 정교하게 반복하는 것.
- 사용자는 환자다. 모델에 안 넘기는 두 가지. (1) 증상의 진실성(날것의 맥락), (2) 최종 동의 — 어느 진단으로 갈지는 사람이 누른다.

## 순서

동작 먼저(못생겨도 됨) → 그다음 theme-factory 스킬로 UI 톤. 검증 안 된 골격에 디자인부터 쓰지 마라.

## 키운 버전 (참고, MVP 아님)

환자 / 의장(House) / 동조·반대·엉뚱 / 레드팀(상시+최종) / 출력 = 가설 + 그걸 죽일 테스트. MVP가 가정을 통과한 뒤에만 짓는다.

## 먼저 확인할 것 (현재 코드 vs 비전)

현 repo는 Python에 `scrapers/`, `profiles/`, `config.yaml`, `static/` 구조다. 지금 polychat이 위 House 비전과 다른 걸(예: 스크래핑 기반 수집) 하고 있을 수 있다. 짓기 전에 `main.py`를 읽고 현 구현과 이 비전을 먼저 대조하라. 새로 짤지, 위에 얹을지는 거기서 결정.
